server:
  port: 8012
spring:
  application:
    name: ch432-eureka-client-provider

eureka:
    client:
      serviceUrl:
        defaultZone: http://localhost:8761/eureka/
    instance:
      prefer-ip-address: true
